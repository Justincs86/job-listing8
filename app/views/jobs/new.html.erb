<h1> Add a Job</h1>

<%= simple_form_for @job do |f| %>

<%= f.input :title %>
<%= f.input :description %>
<%= f.input :lower_wage_bound %>
<%= f.input :upper_wage_bound %>
<%= f.input :contact_email %>
<%= f.input :is_hidden %>

<%= f.submit "Submit", class: "btn btn-success" %>

<% end %>
